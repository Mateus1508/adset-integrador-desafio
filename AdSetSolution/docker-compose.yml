services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2019-latest
    container_name: adset_sqlserver_container
    environment:
      SA_PASSWORD: "AdSet123123"
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"
    networks:
      - app-network

networks:
    app-network:
        driver: bridge